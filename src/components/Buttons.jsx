
// Can not add more options because of API limit :(

/*
import React from 'react'
import Loader from './loader';

const Buttons = ({BASE_URL, endpoint,setEndpoint,isloading}) => {
    const handleMovies = () =>{
        setEndpoint(`${BASE_URL}most-popular-movies`);
      }
      const handleIndianMovies = () =>{
        setEndpoint(`${BASE_URL}india/top-rated-indian-movies`);
      }
      
      const handleTvShows = () => {
        setEndpoint(`${BASE_URL}-most-popular-tv`);
      }
    

  return (
    <div>
         <button className='search text-white cursor-pointer border-2' onClick={handleMovies}>
            { isloading? (<Loader/>): 'Top Popular Movies' } 
          </button>
          <button className='search text-white cursor-pointer border-2' onClick={handleIndianMovies}>
            { isloading? (<Loader/>): 'Top Rated Indian Movies' } 
          </button>
          <button className='search text-white cursor-pointer border-2' onClick={handleTvShows}>
            { isloading? (<Loader/>): 'Top Rated Tv Shows' } 
          </button>

    </div>
  )
}

export default Buttons
*/